 // bool flag = true;
    // while (j - i >= 1) {
    //     if (j - i == 1) { flag = false; break; }

    //     if (a[i] && a[j]) {
    //         i++, j--;
    //     } 
    //     if (!a[i]) i++;
    //     if (!a[j]) j--;
    // }
    // if (flag) cout << "YES" <<ed;
    // else cout << "NO" << ed;